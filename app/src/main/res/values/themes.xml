<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ToDo" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue</item>
        <item name="colorPrimaryVariant">@color/beige</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/blue</item>
        <item name="colorSecondaryVariant">@color/blue</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="windowNoTitle">true</item>
        <item name="textFillColor">@color/black</item>
        <item name="inactiveColor">@color/gray_inactive_light</item>
        <item name="separatorColor">@color/gray</item>
        <item name="infoButtonColor">@color/gray_button_light</item>
        <item name="checkBoxInactiveColor">@color/black_overlay</item>
        <item name="android:windowAnimationStyle">@style/WindowChangeThemeStyle</item>
    </style>
    <style name="ThemeOverlay.ToDo.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/beige</item>
        <item name="fullscreenTextColor">@color/white</item>
    </style>
</resources>